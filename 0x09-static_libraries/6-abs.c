#include "main.h"

/**
 * _abs - computes the absolute value of an interger
 * @n: input number as integer
 * Return: Absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
